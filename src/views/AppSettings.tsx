import { Box, SettingsView } from '@stripe/ui-extension-sdk/ui'

const AppSettings = () => {
  return (
    <SettingsView>
      <Box>Settings</Box>
    </SettingsView>
  )
}

export default AppSettings
